/// DesignSystem — Enterprise-grade UI tokens, components, and modifiers.
///
/// Import this module to access:
/// - **Tokens**: `AppColors`, `AppTypography`, `AppSpacing`, `AppRadius`
/// - **Components**: `AppButton`, `AppCard`, `AppTextField`, `AppAvatar`,
///   `StatusBadge`, `ToastView`, `ShimmerView`, `PulsingDotsView`,
///   `SpinnerOverlayView`, `EmptyStateView`, `AppListRow`
/// - **Modifiers**: `.appCard()`, `.shimmer()`, `.fadeSlideIn()`, `.glowBorder()`,
///   `.toast(_:)`, `.loadingOverlay(_:)`
import SwiftUI
